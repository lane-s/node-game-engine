#User

The user has an id, an associated websocket connection, a list of entity ids that the user owns, and an [InputState](public/InputState.md).

The id property of the connection is set to the user id so that a user can be accessed based on the connection object.

